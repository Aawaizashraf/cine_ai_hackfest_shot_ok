# Logging: DEBUG | INFO | WARNING | ERROR (default: INFO)
# LOG_LEVEL=INFO

# OpenRouter (required for embeddings + optional LLM query parsing)
OPENROUTER_API_KEY=your_openrouter_api_key
# Optional: LLM parses query into intent (default: true). Set false to use raw query only.
# SEARCH_USE_QUERY_LLM=true
# Optional: run intent + raw query and merge with RRF (default: false).
# SEARCH_HYBRID_QUERY=false

# SiliconFlow (optional – for reranking search results; Qwen3-Reranker-8B)
SILICONFLOW_API_KEY=your_siliconflow_api_key
# Optional: only return clips with reranker score >= this (e.g. 0.01 to hide weak matches; 0 = no filter)
# SEARCH_RERANK_MIN_SCORE=0

# Video file for clip playback (optional; default: project folder The Godfather (1972) [1080p]/...mp4)
# VIDEO_PATH=/path/to/The.Godfather.1972.1080p.BrRip.x264.BOKUTOX.YIFY.mp4

# Qdrant – local or cloud (no API key for local)
# Local:  QDRANT_URL=http://localhost:6333  QDRANT_API_KEY=
# Cloud:  use your cluster URL and API key from https://cloud.qdrant.io
QDRANT_URL=https://your-cluster-id.xxx.gcp.cloud.qdrant.io
QDRANT_API_KEY=your_qdrant_cloud_api_key
